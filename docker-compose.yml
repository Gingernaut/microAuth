  version: '3.4'
  services:
    database:
      image: postgres:10.3
      ports:
      - "5432:5432"
      environment:
        POSTGRES_USER: "${TEST_DB_USERNAME}"
        POSTGRES_PASSWORD: "${TEST_DB_PASSWORD}"
    microauth:
      build: .
      ports:
        - "5000:5000"
      depends_on:
        - database